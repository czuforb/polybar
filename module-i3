type = internal/i3
format = <label-state> <label-mode>
index-sort = true
wrapping-scroll = false

label-mode-padding = 10

; focused = Active workspace on focused monitor
label-focused = %icon% %index%
label-focused-foreground = ${gruvbox.bg-dark}


; label-focused-background = ${gruvbox.green}
label-focused-background = ${gruvbox.yellow}
label-focused-padding = 2

; unfocused = Inactive workspace on any monitor
label-unfocused = %index%
label-unfocused-padding = ${self.label-focused-padding}
label-unfocused-foreground = ${colors.foreground}
label-unfocused-background = #000

; visible = Active workspace on unfocused monitor
label-visible = %icon% %index%
label-visible-background = ${gruvbox.aqua}
label-visible-foreground = ${gruvbox.yellow}

; urgent = Workspace with urgency hint set
label-urgent = %index%
label-urgent-background = ${colors.alert}
label-urgent-padding = ${self.label-focused-padding}

